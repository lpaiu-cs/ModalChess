output_dir: outputs/eval
model_config_paths:
  - configs/model/board_encoder.yaml
  - configs/model/heads.yaml
dataset:
  source: fixture
  history_length: 1
  limit: null
  split: all
model: {}
metrics:
  topk: [1, 3, 5]
  batch_size: 32
